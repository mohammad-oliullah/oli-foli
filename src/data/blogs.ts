import type { BlogPost } from "@/types/blog";

export const blogs: readonly BlogPost[] = [];

export const blogsNote =
  "No articles are listed yet. When you publish, add entries in src/data/blogs.ts (title, excerpt, date, category, tags, URL, reading time). This page will not invent posts.";
